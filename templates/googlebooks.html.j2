<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
</head>

<body>
    <button class="btn btn-primary fw-bold float-end mr-5">Add book</button>
    <h1 class="text-center mt-5">Library</h1>
    <div class="books mt-5 d-flex flex-wrap">
        {% for book in books %}
        <div class="card m-5 
        " style="width: 20rem;">
            <img class="card-img-top" src={{book.cover}} alt="Card image cap">
            <div class="card-body text-center bg-danger p-0">
                <h5 class="card-title">{{book.title}}</h5>
                {# <p class="card-text">{{book.description}}</p> #}
                <a href="#" class="btn btn-primary w-100">View</a>
            </div>
        </div>
        {% endfor %}
    </div>
</body>

</html>